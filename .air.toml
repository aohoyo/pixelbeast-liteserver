# 像素兽 - Air 热重载配置

root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./pixelbeast"
  bin = "./pixelbeast"
  args = ["-config", "config"]
  include_ext = ["go", "html", "css", "js"]
  exclude_ext = ["_test.go"]
  include_dir = ["src"]
  exclude_dir = ["tmp", "vendor", "testdata", "sites", "ftp", "log", "docs", "node_modules"]
  delay = 1000
  stop_on_root = true
  send_interrupt = true
  kill_delay = 500

[log]
  time = true
  main_only = false

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"

[misc]
  clean_on_exit = true

[screen]
  clear_on_rebuild = false
  keep_scroll = true
